[project]
name = "stage-0"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.135.3",
    "httpx>=0.28.1",
]

[dependency-groups]
dev = [
    "uvicorn>=0.44.0",
]
